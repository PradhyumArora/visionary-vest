/** @type {import('next').NextConfig} */
const nextConfig = {
     images: {
    domains: ['thispersondoesnotexist.com'], // Replace 'example.com' with your desired domain
  },
};

export default nextConfig;
